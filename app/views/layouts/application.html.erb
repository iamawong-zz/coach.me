<!DOCTYPE html>
<html>
<head>
  <title>Coach.me</title>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="header-frame">
    <%= link_to image_tag("logo.png"), root_path %>
    <a class="about">About</a>
    <a class="find">Find</a>
    <% if user_signed_in? %>
      <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
    <% else %>
      <%= link_to "Join", new_user_registration_path %> | <%= link_to "Login", new_user_session_path %>
    <% end %>
  </div>

  <% # Something that devise wanted me to add, will look more into this later. %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <div id="content">
    <%= yield %>
  </div>

  <div id="footer">
  </div>
</body>
</html>
